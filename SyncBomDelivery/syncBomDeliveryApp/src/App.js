import React from 'react';
import {Root} from 'native-base';
import {MainNavigator} from './navigators';

const App = () => {
  return (
    <Root>
      <MainNavigator />
    </Root>
  );
};

export default App;
